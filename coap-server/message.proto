syntax = "proto3";

/* If editing this file, also remember to update message.proto under
   the 'node' directory
   
   @TODO synchronize these two files */

message SensorValues {
    uint32 sensor_id = 1;
    float temperature = 2;
    uint32 pressure = 3;
}